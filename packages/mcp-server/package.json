{
  "name": "@kiro/mcp-server",
  "version": "0.0.5",
  "private": true,
  "main": "./out/server.js",
  "scripts": {
    "build": "esbuild src/server.ts --bundle --platform=node --format=cjs --outfile=out/server.js",
    "watch": "npm run build -- --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.17.5"
  }
}

